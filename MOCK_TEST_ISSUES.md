# 모의고사 시스템 정밀 진단 및 해결책 보고서 (V1.1)

## 1. 개요
현재 실전 모의고사(1회, 2회)의 데이터 처리 과정에서 심각한 단절이 발견되었습니다. 

*   **정상 작동:** 학생의 시험 직후 결과 페이지 (메모리 내 데이터를 즉시 계산하므로 정상 표기됨)
*   **비정상 작동:** 
    1.  **관리자 리포트 페이지:** 데이터 집계가 0으로 나오거나 파트별 통계 누락
    2.  **나의 학습방(대시보드):** 시험 이력 및 점수 반영 안 됨
    3.  **약점 분석(위크니스):** 데이터가 없어 "기록 없음"으로 표시

---

## 2. 핵심 문제점 진단

### [원인 A] 관리자 리포트의 '재계산' 실패 (Missing Firestore Data)
관리자 리포트는 DB에 저장된 `answers`를 가져와 `calculateMockScore` 엔진으로 재계계산을 시도합니다.

*   **현상:** 과거 저장 로직에서 `answers` 필드가 누락되었거나, 혹은 `testId`가 숫자/문자열 타입 불일치로 인해 정답지 로드에 실패함.
*   **결과:** 재계산 엔진이 빈 답안지(`{}`)를 받은 것으로 처리하여 모든 점수가 0점으로 표시됨.

### [원인 B] ID 추출 로직의 설계 결함 (Manager_Results 증발)
대시보드와 약점 분석용 데이터를 생성하는 로직이 모의고사 고유 ID와 충돌합니다.

*   **현상:** `p5-t9-q101` ID에서 숫자만 추출할 때 `replace(/[^0-9]/g, '')`가 작동하여 **`59101`** 생성.
*   **결과:** 파트 판별 범위(101~130)을 한참 벗어나므로, `belongs = false`가 되어 `Manager_Results` 컬렉션에 데이터가 **아예 생성되지 않음**.
*   **영향:** 대시보드 엔진은 분석할 원천 소스(`Manager_Results`)가 없어 아무것도 보여주지 못함.

---

## 3. 제안하는 해결책 (Action Plan)

### [해결책 1] ID 판별 로직의 정밀화 (Regex Refactoring)
숫자만 무조건 뽑는 방식에서, ID의 마지막 번호만 가져오도록 정규식 수정.
```typescript
// TO-BE: 가장 마지막의 숫자만 그룹화하여 추출
const match = qId.match(/q(\d+)$/);
const qNum = match ? parseInt(match[1]) : 0;
```

### [해결책 2] 저장 시점의 데이터 강제 주입 (Save Logic Fix)
시험 종료 시 `MockTestAttempts`에 `answers` 원본 트리를 반드시 포함하고, `Manager_Results` 생성 실패 시에도 최소한의 로그를 남기도록 보완.

### [해결책 3] 채점 엔진의 이원화 및 표준화
모의고사는 1~200번의 고정 번호 체계로, 하프테스트는 가변 번호 체계로 완전히 분리하여 로직 간섭 방지.

---

## 4. 기대 효과
*   **관리자 신뢰도 회복:** 관리자 페이지에서 학생의 실제 실력이 정확히 노출됨.
*   **학습 데이터 통합:** 학생이 푼 모의고사 데이터가 약점 분석에 포함되어 유의미한 AI 피드백 제공 가능.
*   **데이터 정합성:** 학생 페이지와 관리자 페이지의 점수 일치.

---
**보고자:** Antigravity AI Assistant
**날짜:** 2026-02-01
