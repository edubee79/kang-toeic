# Part 7 데이터 및 아키텍처 원상복구 계획서

**목적**: AI의 독단적인 설계 변경으로 인한 Part 7 데이터 연결 오류를 해결하고, 사용자님의 원본 '파일 분리형 관리 구조'를 100% 복원함.

---

## [단계별 작업 계획]

### 1단계: 원본 데이터(이중/삼중 지문) 건전성 검사
- **대상**: `src/data/rc_part7_test1.ts` ~ `rc_part7_test10.ts` (총 10개 파일)
- **작업 내용**: 
    - 각 파일의 존재 유무 확인.
    - 176번~200번 문항 데이터가 온전한지 전수 조사.
- **성공 가이드**: 10개 파일 모두 정상 확인 및 데이터 보존 확인 시 보고.

### 2단계: 싱글 패시지 파일 규격 정규화
- **대상**: `src/data/toeic/reading/part7/test1.ts` ~ `test9.ts`
- **작업 내용**: 
    - 현재 통합본으로 되어있는 `test9.ts`에서 이중지문 데이터 제거 (싱글 전용으로 변경).
    - `test1~8.ts` 파일들이 싱글 패시지(147-175) 규격에 맞게 정렬되었는지 확인.
- **성공 가이드**: 모든 메인 데이터 파일이 '싱글 패시지 전용'으로 슬림화되면 보고.

### 3단계: 시스템 연결 로직 원상복구
- **대상**: `src/data/mock-test-data.ts`
- **작업 내용**:
    - AI가 임의로 추가한 147-175 필터링 코드 완전 제거.
    - 싱글 데이터(`test*.ts`)와 이중지문 데이터(`rc_part7_test*.ts`)를 시스템상에서 다시 매핑(Mapping).
- **성공 가이드**: 빌드 오류 없이 데이터가 정상적으로 조립되면 보고.

### 4단계: 실시간 렌더링 및 무결성 검증
- **대상**: 모의고사 1회 ~ 10회 (브라우저 확인)
- **작업 내용**:
    - 각 회차당 200번까지 모든 문항이 출력되는지 확인.
    - 특히 176번 이후의 이중/삼중 지문 레이아웃이 깨지지 않는지 확인.
- **성공 가이드**: 전체 문항 정상 출력 확인 시 보고.

### 5단계: 최종 정리 및 작업 종료
- **작업 내용**: 임시 파일 정리 및 최종 결과 보고.

---
**주의 사항**: 각 단계가 완료될 때마다 사용자님께 상세 보고를 올리고, 승인을 받은 후 다음 단계로 이행함.
