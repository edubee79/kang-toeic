# 📊 강TOEIC 시스템 데이터 및 로직 정합성 감사 보고서

| 항목 | 상태 | 주요 발견 사항 |
| :--- | :---: | :--- |
| **Data Consistency** | 🔴 심각 | 파일별 필드명(`answer` vs `correctAnswer`), 옵션 구조(`string[]` vs `object[]`) 불일치 |
| **Naming Convention** | 🟡 주의 | 문항 ID 체계 혼재 (`q101`, `RT1_101`, `101`), 파트별 키값 불일치 |
| **Tagging Accuracy** | 🔴 심각 | Part 5 정밀 태깅 오분류 사례 다수 발견 (예: 능동태를 수동태 `v3`로 마킹) |
| **Calculation Logic** | 🟡 주의 | 하프테스트(100문항)와 풀세트(200문항) 간의 `PART_MAX` 가중치 충돌 위험 |

---

## 1. 🚨 데이터 오염 및 정합성 불일치 사례

### 1-1. 필드명 및 자료형 불일치
기존 `tests.ts`와 최근 생성된 `rc_part7_test10.ts` 간의 매핑 구조가 완전히 다릅니다. 이로 인해 채점 엔진이 특정 파일에서 작동하지 않거나 점수를 0점으로 처리할 우려가 큽니다.

*   **정답 필드**: `correctAnswer` (표준) ↔ `answer` (오염: `test10-176-180` 등)
*   **옵션 구조**: `{label: "A", text: "..."}` (표준) ↔ `"(A) text"` (오염: `rc_part7_test10.ts` 등은 문자열 배열 사용)
*   **태그 누락**: 최근 추가된 연계 지문 데이터(Part 7)에서 `classification` 필드가 누락되어 AI 취약점 분석이 "분석 불가"로 출력됨.

### 1-2. 문항 ID 체계의 파편화
데이터 정제 프로세스 중단으로 인해 문항을 식별하는 고유 ID가 제각각입니다.
*   **사례 1 (Real Test)**: `id: "RT1_101"`
*   **사례 2 (Practice)**: `id: "q1227"`
*   **사례 3 (Level Test)**: `id: "176"`
*   **리스크**: 오답 기록 관리 및 중복 제어 로직에서 일관된 패턴 매칭이 불가능함.

---

## 2. 🧠 계산 로직 및 서비스 엔진 문제

### 2-1. `goalAnalysisService.ts`의 하드코딩 리스크
현재 성적 분석 엔진에 정의된 `PART_MAX` 값이 **풀세트(200문항)** 기준으로 고정되어 있습니다.
*   **현상**: 파트 5의 문항 수를 항상 30개로 간주함.
*   **위험**: 현재 작업 중인 **하프테스트(15문항)** 결과를 입력할 경우, 15개를 다 맞아도 달성률이 50%로 계산되는 수치 왜곡 발생.

### 2-2. 용어 정의의 모호성 (Part 7)
시스템 전반에서 Part 7을 부르는 명칭이 통일되지 않아 데이터가 누락되고 있습니다.
*   **혼종 사용**: `part7_test`, `part7_single`, `part7_double`, `p7_single`, `p7`
*   **결과**: 관리자 페이지와 학생 대시보드 간의 집계 수치가 일치하지 않는 원인이 됨.

---

## 3. 🎯 Part 5 패턴 마킹(Tagging) 오염 실태

표준 규격(`toeic-standards.ts`) 확립 전후의 데이터가 섞여 있어 AI 분석의 신뢰도가 낮습니다.

*   **오분류 사례**: `src/data/toeic/reading/part5/tests.ts`의 `q104`번
    *   **실제 문법**: "will be sharing" (미래 진행형 - **능동**)
    *   **마킹된 태그**: `v3` (**수동태 세부 형태**)
    *   **결과**: 학생이 정답을 맞춰도 AI는 "수동태 이해도가 높음"이라는 엉뚱한 로그를 남기게 됨.

---

## 4. 🏠 나의 학습방 및 성적 연동 시스템 분석

### 4-1. 점수 설정 및 저장 구조 (`Winter_Users`)
*   **저장 위치**: Firestore `Winter_Users/{userId}`
*   **사용 필드**: `targetScore` (총점), `targetLC`, `targetRC`, `partTargets` (파트별 목표 개수)
*   **문제점**: 목표 점수를 수정할 때 `AI 자동 배분` 로직이 클라이언트(`StudentDashboard`)에 하드코딩되어 있어, 나중에 배분 규칙이 변경될 경우 모든 페이지를 찾아 수정해야 함.

### 4-2. 성적 산출 및 반영 프로세스 (Data Flow)
현재 성적을 가져오는 경로가 3가지로 파편화되어 있어 수치 불일치가 필연적입니다.

1.  **경로 A (대시보드 통계)**: `fetchStats` 함수 내부에서 `Manager_Results`를 직접 쿼리하여 계산.
2.  **경로 B (약점 분석 리포트)**: `WeaknessService` → `analyzeGoalStatus`를 거쳐 가중치가 적용된 결과 반환.
3.  **경로 C (관리자 페이지)**: `AdminDashboard` 내부에서 자체적으로 로그를 카운팅하여 표시.

**발견된 패턴 및 함수명:**
*   `fetchStats`: `Manager_Results`에서 `type`별 고유 `unit` 개수를 카운트 (단순 진행률용).
*   `calculateActualTestStats`: 실전 모의고사만 필터링하여 `latest`, `average` 산출.
*   `getUserProfile`: 원본 사용자 목표 데이터를 조회.
*   **환산 공식**: `(LC_맞은개수 * 5) + 10` 및 `(RC_맞은개수 * 5) - 10`

### 4-3. 수치 반영의 기술적 오류 (Logic Conflict)
*   **필드명 불일치**: 
    *   서비스 레이어/UI: `p1`, `p2`, `p3`, `p4`... (Short key)
    *   DB/일부 로직: `part1_test`, `part2_test`... (Full key)
    *   **결과**: 키 매칭 실패로 인해 시험 기록이 있어도 '평균/최근 점수'가 0으로 표기되는 지점 발생.
*   **가중치(PART_MAX) 중복 정의**:
    *   `page.tsx`, `goalAnalysisService.ts`, `WeaknessService.ts` 3곳에 파트별 만점 개수가 각각 정의되어 있어 유지보수가 매우 위험함.

---

## 5. 👨‍💼 페이지 간 정보 공유 및 연동성 점검

### 5-1. 나의 학습방 ↔ Weakness 분석
*   현재 두 페이지는 `WeaknessService`를 공유하고 있어 데이터 일관성은 양호함. 
*   단, **"최근 점수(Latest)"** 반영 시 `createdAt` 필드가 없는 구형 데이터가 필터링에서 누락되는 현상이 보임.

### 5-2. 관리자 페이지 ↔ 학생 페이지 (심각한 격차)
*   **관리자 페이지**: 단순히 로그의 개수를 세어서 "몇 회 풀었음"만 표시 (진행률 중심).
*   **학생 페이지**: 실제 맞은 개수와 목표 대비 달성률을 계산하여 표시 (성취도 중심).
*   **문제**: 관리자는 학생이 "참여를 잘하고 있다"고 보는데, 학생 대시보드에는 "목표 달성 실패"로 나오는 수치적 괴리 발생.

---

## 6. 🚀 향후 개선 방안 (Proposed Architecture)

1.  **Centralized Score Engine**: 모든 점수 계산(환산식, 가중치, 목표 대비 차이)을 `ScoreCalculator` 클래스로 단일화하여 공유.
2.  **Standard Key System**: 내부 통신 키를 `p1~p7`의 단축키로 강제 통일하고, DB 입출력 시에만 어댑터를 통해 변환.
3.  **Admin Dashboard 연동 고도화**: 관리자용 학생 상세 보기에서도 학생 대시보드와 동일한 **'성취도 그래프'**와 **'AI 예측 점수'**를 볼 수 있도록 로직 통합.
4.  **Data Normalization**: `Manager_Results`에 기록되는 모든 필드명을 표준 규격(`correctAnswer`, `classification`)으로 통일하는 일차적 청소 작업 선행.

---
**최종 업데이트**: 2026-01-25  
**작성자**: Antigravity AI Assistant
